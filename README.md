## CI/CD & Testing

The project uses GitHub Actions for basic CI workflow automation.  
On each push, the workflow runs HTML validation checks to verify that the project follows standard HTML syntax and structure requirements.
